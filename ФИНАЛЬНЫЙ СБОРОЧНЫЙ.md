Теперь собери все компоненты вместе в docker-compose.yml для разработки:

services:
  - db (PostgreSQL 15)
  - backend (Django + gunicorn)
  - frontend (Vue dev server с hot-reload)
  - nginx (для проксирования запросов)

Добавь:
- Makefile с командами: make build, make up, make migrate, make createsuperuser
- .env.example с переменными окружения
- pre-commit конфиг с линтерами (black, flake8, eslint)
- README.md с инструкцией по запуску

Также создай скрипт seed_data.py для заполнения базы тестовыми данными:
- 3 проекта
- 15+ находок разных severity
- 2 шаблона отчетов